

Collection Framework

1. List Interface (Allow duplicate elements and one null value)
    1.1 ArrayList ///stores in different index (will not preserve the insertion order)
    1.2 LinkedList (will store insertion order)
    1.3 Vector
2. Set interface
    2.1 Hash set
    2.2 LinkedHashSet
    2.3 TreeSet
3. Queue interface
    3.1 Priority Queue


    -All Collection class doesn't allow primitive data type..
    -you must use wrapper class.(creates an instance)

    int = Integer
    long = Long
    boolean = Boolean

    -its resizeable array so it can hold elements we are adding.

    Syntax :
        Name of Interface or Class
        <>with data types
        = new And Class name.

        //E.g
        List<String> list = new ArrayList<String>();

        this is variable called list = it can hold String data(any amount)

